{
  "name": "@owlfolio/worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts --once --dry-run --define-defaults",
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@owlfolio/ledger": "workspace:*",
    "@owlfolio/providers": "workspace:*",
    "@owlfolio/shared": "workspace:*",
    "@owlfolio/strategies": "workspace:*",
    "@owlfolio/workflow": "workspace:*"
  },
  "devDependencies": {
    "tsx": "4.21.0"
  }
}
